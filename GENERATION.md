# 미디어 생성: study2 대비 달라진 점

영상 개요, 오디오 개요, 인포그래픽을 어떻게 만드는지, 그리고 study2(`reading-experiment-mba`)와
무엇이 다른지 기록하는 문서입니다. 보고용이며, 파이프라인을 바꿀 때마다 갱신합니다.
변경 기록은 아래로 갈수록 최신입니다.

## study2와 비교

| | study2 | 이 연구 (`study-ug`) |
|---|---|---|
| 논문 | 참가자가 각자 자기 논문을 업로드 | 모두 같은 논문 1편 (Häfner, Stock & Oberst 2014) |
| 생성 시점 | 참가자마다 업로드 직후 생성 (약 5~6분) | 기본 콘텐츠는 한 번 만들어 모두에게 제공. 커스터마이즈 조건만 모달리티당 1회 재생성 |
| 오디오 | GPT-4o가 2인 대담 대본 작성 → OpenAI `tts-1`로 대사마다 합성해 이어붙임 | 구조는 아직 같음 (변경 기록 참고) |
| 영상 | 나레이션 슬라이드쇼. Gemini 2.0 Flash가 약 30장면 구성 → `gemini-2.5-flash-image`가 장면마다 슬라이드 → OpenAI `tts-1-hd`가 장면마다 나레이션. (Veo 2 함수도 있었으나 화면에서 쓰지 않음) | 구조는 아직 같음 (변경 기록 참고) |
| 인포그래픽 | `gemini-3-pro-image-preview` 1회 호출 | 아직 같음 |
| 모델에 준 논문 분량 | 오디오·영상·인포그래픽 순으로 앞 30,000 / 25,000 / 15,000자. 자른 이유는 기록 없음 | 물려받았으나, **영상과 인포그래픽이 결과·논의 절을 못 보게 만든다는 것을 확인** (발견 사항 참고) |
| 결과물 구조 | 도입→배경→방법→결과→의미로 고정. 서로 다른 논문의 결과물을 비교하기 위해서 | 물려받았으나, 규칙이 아니라 기본값으로 바꾸기로 함 (결정 사항 참고) |
| 챗봇 | `gpt-4o-mini`, 업로드한 PDF 원문 추출 텍스트 | `gpt-4o-mini` (재검토 중), 생성기와 같은 정리본 |

## 변경 기록

**2026-09-24 — study2 파이프라인 이식**
- 세 생성기를 `functions/generation.js`로 옮김. Firebase·HTTP 코드와 분리해서 기본 콘텐츠와
  커스터마이즈 생성이 같은 코드를 쓰도록 함.
- 논문을 매번 업로드하지 않고, 추출한 텍스트를 함수와 함께 배포.
- study2가 쓰던 모델 두 개가 폐기되어 교체: `gemini-2.0-flash` → `gemini-3.6-flash`(영상 구성),
  `gemini-2.5-flash-image` → `gemini-3.1-flash-image`(슬라이드).
- 장면 구성 JSON이 길면 중간에 잘려 전체가 실패하던 문제 → 온전히 받은 장면까지 살려 씀.
- 슬라이드 속 글자가 자주 틀림("SITRESS", "Burmout") → 장면 하나만 다시 그리는
  `scripts/fix-scenes.js` 추가.
- 영상을 base64 JSON 하나 대신 장면별 이미지·나레이션 파일과 목록 파일로 제공.
- 영상 길이 추정 수정: 나레이션은 장면당 약 5.7초로, 구성 단계가 계획한 9초가 아님.
- 초기 테스트용으로 넣었던 NotebookLM 파일을 파이프라인 결과물로 교체. 모든 조건이 같은
  생성기의 콘텐츠를 보도록.

**2026-09-24~25 — 커스터마이즈**
- `generateCustom` 함수 추가: 커스터마이즈 조건은 모달리티당 1회 재생성. 페이지가 작업 상태를
  지켜보는 방식이라 진행 표시, 중단, 새로고침이 가능. 실패·중단은 횟수에서 차감하지 않음.
- 참가자 요청은 인용된 데이터로 프롬프트에 추가. 처음엔 논문 앞에 두고 선택 사항처럼
  표현했으나, 논문 뒤로 옮기고 요구사항으로 표현을 바꿈.
- HTTP 429·5xx뿐 아니라 연결 끊김도 재시도.
- 음성 합성을 하나씩이 아니라 6개씩 동시에 (팟캐스트 44초 → 19초).
- 생성물마다 `record.json` 저장: 요청, 조건, 사용 모델, 전체 대본·나레이션·슬라이드 글자.
  나중에 정확성 평가용.

**2026-09-25 — 커스터마이즈 화면**
- 자유 입력은 **내용만** (무엇에 집중할지, 어떻게 설명할지). 목소리·속도·색상·그림체 요청은
  반영되지 않을 수 있다고 경고.
- 모달리티마다 버튼 설정 하나: 영상·오디오는 길이, 인포그래픽은 디테일 수준.

**2026-09-26 — 1단계: 논문 입력 정리, 전문 전달**
- study2의 분량 제한(앞 15,000 / 25,000 / 30,000자)을 없애고 **세 모달리티 모두 논문 전체**를 받도록 함.
- 논문 텍스트를 새로 만듦 (51,017자 → 38,482자). PDF를 글자 위치 기준으로 줄 단위로 다시 뽑아
  (`scripts/paper/extract-lines.js`) 정리함 (`scripts/paper/build-text.py`).
  - 제거: 쪽 머리말("82 A. Häfner et al." 등), 저널·DOI·저작권·소속 줄, **참고문헌 목록과 저자 소개**
    (전체의 약 25%. 인명·연도가 결과로 오인될 수 있음).
  - 복원: 줄바꿈으로 끊긴 단어 연결 (진짜 하이픈인 "Stewart-Brown", "time-related"만 유지),
    들여쓰기로 문단 구분, 절 제목(Method, Results 등).
  - **표 6개를 표 형태로 재구성.** 기존 추출은 표를 문단 사이에 한 줄로 풀어 숫자가 뒤섞였음.
    숫자는 다시 입력하지 않고 PDF 행에서 그대로 읽음. 표 1–2는 표본 절 끝, 표 3–6은 결과 절 끝에 배치.
  - 검증: 원본 본문의 4글자 이상 단어 3,374개가 모두 정리본에 있음. 원문의 오타도 그대로 둠.
- **챗봇도 같은 정리본을 읽도록 변경.** 이전 챗봇은 브라우저에서 PDF를 그대로 추출한 원문
  전체(51,017자, 14쪽 모두)를 받았음. 잘림은 없었으나 쪽 머리말, 참고문헌, 풀어진 표가 섞여 있었음.
  (프롬프트에 넣기 전 HTML 태그 제거 정규식이 `p<0.05` 같은 표기를 지울 위험도 확인했으나,
  논문에 `>` 문자가 없어 실제로 지워진 글자는 0자.) 이제 생성기와 챗봇이 같은 텍스트
  (`docs/papers/hafner2014/paper.txt`, 함수용 사본 `functions/paper-hafner2014.txt`)를 씀.

## 발견 사항 (2026-09-26)

1. **영상과 인포그래픽은 결과를 보지 못하고 만들어졌다.** study2에서 물려받은 분량 제한 때문에
   인포그래픽은 51,017자 중 앞 15,000자, 영상은 25,000자만 받는다. 결과 절은 26,018자,
   논의 절은 29,329자에서 시작한다. 오디오(30,000자)는 결과는 보지만 논의 절 대부분이 잘린다.
2. **결과를 못 본 영상 나레이션에 논문에 없는 주장이 있다.** 결과를 본 오디오에는 없다.
   기본 영상의 예:
   - "대학생의 최대 75%가 중간 수준 스트레스를 겪는다" → 논문은 *다른 연구*의 *커뮤니티 칼리지*
     학생 212명 중 75%가 *최소* 중간 수준이라고 인용
   - "시간 통제감이 유의하게 증가했다" → 4주에서만 유의, 2주에서는 경향만
   - "요구가 완벽하게 일정했고 실제 수업 난이도는 변하지 않았다" → *지각된*(자기보고) 요구이고,
     변화가 유의하지 않았을 뿐
   - "불안과 정신적 피로가 줄었다" → 측정하지 않음. 측정한 것은 tension
3. **논문 밖 내용을 쓰지 말라는 지시가 어느 프롬프트에도 없다.** 입력에는 참고문헌(약 25%),
   쪽 머리말, 문단 사이에 풀어진 표가 섞여 있고, temperature는 0.7이다.
4. **요청 반영이 약하고, 오디오가 가장 심하다.** study2 프롬프트가 고정 구조를 필수로 요구하고,
   요청은 "위 규칙과 충돌하지 않는 범위에서"만 따르게 되어 있다. 그래서 한 부분에 집중해 달라는
   요청이 모든 부분을 다루라는 규칙에 밀린다. 약 1.5분짜리 팟캐스트는 인사말에도 몇 줄을 쓴다.
5. **모델이 오래됐고 제조사가 섞여 있다.** 챗봇 `gpt-4o-mini`, 오디오 대본 `gpt-4o`, 음성
   `tts-1`은 2023~2024년 모델이다. 오디오는 OpenAI, 영상·인포그래픽은 Google이라 모달리티와
   제조사가 뒤섞인다.

## 결정 사항 (2026-09-26)

- **구조는 규칙이 아니라 기본값.** study2는 서로 다른 논문의 결과물을 비교하려고 구조를 고정했다.
  논문이 하나이고 커스터마이즈가 있는 지금은, 요청이 구성을 결정하고 기본 구조는 요청이 비워 둔
  부분만 채운다.
- **논문을 자르지 않는다.** 참고문헌, 쪽 머리말, 풀어진 표를 정리한 전문을 모든 단계에 준다.
  산출물에 반드시 넣을 항목 목록은 따로 두지 않는다.
- **기본 콘텐츠 목표 길이: 영상 5분, 오디오 2분** (조정 가능). 커스터마이즈에서 "기본"을 고르면
  이 길이에서 ±30초 안이면 된다. 정확히 같을 필요는 없다.
- **설정:** 영상·오디오는 길이만 (짧게 / 기본 / 길게), 인포그래픽은 디테일 수준 (간단하게 / 기본 / 깊게).
- **모델은 비교해서 고른다.** 입력과 프롬프트를 정리한 뒤, 후보 모델 조합들을 같은 논문으로 돌려
  논문 대비 정확도, 요청 반영도, 생성 시간으로 비교한다.
- **지금 기본 콘텐츠는 임시본.** 최종 파이프라인으로 다시 만든다.
- **생성 시간은 마지막에.** 모델 선택에 따라 달라지므로.

## 작업 순서

1. ~~논문 텍스트 정리, 전문 전달~~ (완료 2026-09-26)
2. 프롬프트 재설계: 근거 규칙, temperature 낮추기, 요청 우선 구조, 상대적 길이·디테일 설정, 영상 장면 수
3. 후보 모델 조합 비교
4. 모델 결정 (챗봇 교체 여부 포함)
5. 세부 조정 후 기본 콘텐츠 재생성
6. 생성 시간
